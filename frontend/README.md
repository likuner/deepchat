# Frontend

Next.js App Router frontend for the DeepSeek-style chat app.

## Run

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Environment

Create `.env.local` if the backend is not running at the default URL.

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
